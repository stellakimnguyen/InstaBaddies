language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - npm install
  - cd server && npm install
  - cd ../client && npm install
  - cd .. && npm run start
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: build
  on:
    branches:
      only:
        - master
        - instabaddAttempt2