- To run with a database, you must follow instructions on creating an account onf MongoDB and obtaining your connection string then placing that string in the MONGODB_URL environment variable

``` npm install ```
``` npm run start ```
